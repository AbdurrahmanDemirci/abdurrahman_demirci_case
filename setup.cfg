[tool:pytest]
# Ensures project root is in sys.path so all package imports resolve correctly
pythonpath = .

[flake8]
max-line-length = 100
extend-ignore = E221
exclude = .git,__pycache__,.env,automation-test-results,venv,.venv
